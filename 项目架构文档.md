# 电影交流社区后端项目架构文档

> **最后更新时间**: 2026-02-26  
> **项目版本**: v0.0.1-SNAPSHOT  
> **Spring Boot版本**: 4.0.3  
> **Java版本**: 17

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构](#项目结构)
4. [数据库设计](#数据库设计)
5. [核心模块说明](#核心模块说明)
6. [API接口列表](#api接口列表)
7. [配置说明](#配置说明)
8. [开发规范](#开发规范)

---

## 项目概述

这是一个基于 **Flutter** 前端和 **Spring Boot** 后端的电影交流社区系统。用户可以浏览电影信息、发布动态、参与活动、收藏电影等。

### 主要功能模块

- **用户系统**: 注册、登录、个人信息管理
- **电影信息**: 集成TMDB API，提供电影数据查询
- **社区动态**: 用户发布影评、评分、互动（点赞、评论、分享）
- **活动管理**: 观影团、电影节等线下活动
- **收藏系统**: 用户收藏喜欢的电影

---

## 技术栈

### 后端框架
- **Spring Boot 4.0.3**: 主框架
- **Spring Data JPA**: 数据持久化
- **Spring Security**: 安全认证
- **MySQL 8.0+**: 关系型数据库
- **Lombok**: 简化Java代码

### 开发工具
- **Maven**: 项目构建管理
- **阿里云Maven镜像**: 加速依赖下载

### 第三方集成
- **TMDB API**: 电影数据库接口
- **JWT**: Token认证（配置中）

---

## 项目结构

```
java_community_backend/
├── src/
│   ├── main/
│   │   ├── java/com/community/java_community_backend/
│   │   │   ├── JavaCommunityBackendApplication.java    # 主启动类
│   │   │   ├── controller/                             # 控制器层
│   │   │   │   ├── HelloController.java                # 测试控制器
│   │   │   │   └── TmdbController.java                 # TMDB API控制器
│   │   │   ├── service/                                # 服务层
│   │   │   │   └── TmdbService.java                    # TMDB服务
│   │   │   ├── repository/                             # 数据访问层
│   │   │   │   ├── UserRepository.java                 # 用户仓库
│   │   │   │   ├── MovieRepository.java                # 电影仓库
│   │   │   │   ├── FeedRepository.java                 # 动态仓库
│   │   │   │   ├── CommentRepository.java              # 评论仓库
│   │   │   │   ├── LikeRepository.java                 # 点赞仓库
│   │   │   │   ├── FavoriteRepository.java             # 收藏仓库
│   │   │   │   ├── EventRepository.java                # 活动仓库
│   │   │   │   └── EventParticipantRepository.java     # 活动参与仓库
│   │   │   ├── entity/                                 # 实体类
│   │   │   │   ├── User.java                           # 用户实体
│   │   │   │   ├── Movie.java                          # 电影实体
│   │   │   │   ├── Feed.java                           # 动态实体
│   │   │   │   ├── Comment.java                        # 评论实体
│   │   │   │   ├── Like.java                           # 点赞实体
│   │   │   │   ├── Favorite.java                       # 收藏实体
│   │   │   │   ├── Event.java                          # 活动实体
│   │   │   │   └── EventParticipant.java               # 活动参与实体
│   │   │   ├── dto/                                    # 数据传输对象
│   │   │   │   └── response/
│   │   │   │       └── ApiResponse.java                # 统一响应格式
│   │   │   ├── Security/                               # 安全配置
│   │   │   │   └── SecurityConfig.java                 # Spring Security配置
│   │   │   └── util/                                   # 工具类
│   │   │       └── TimeUtil.java                       # 时间工具类
│   │   └── resources/
│   │       ├── application.properties                  # 应用配置文件
│   │       ├── static/                                 # 静态资源
│   │       └── templates/                              # 模板文件
│   └── test/                                           # 测试代码
├── pom.xml                                             # Maven配置
├── 配置说明.md                                          # 配置说明文档
├── 项目架构文档.md                                      # 本文档
├── 后端API文档.md                                       # API接口文档
├── TMDB集成使用指南.md                                  # TMDB集成指南
└── 项目规则文档.md                                      # 开发规则文档
```

---

## 数据库设计

### 数据库信息
- **数据库名**: `final`
- **字符集**: `utf8mb4`
- **排序规则**: `utf8mb4_0900_ai_ci`

### 数据表结构

#### 1. users（用户表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| username | VARCHAR(50) | 用户名 | UNIQUE, NOT NULL |
| phone | VARCHAR(11) | 手机号 | UNIQUE, NOT NULL |
| password | VARCHAR(255) | 密码（加密） | NOT NULL |
| avatar | VARCHAR(500) | 头像URL | NULL |
| created_at | DATETIME | 创建时间 | NOT NULL |
| updated_at | DATETIME | 更新时间 | NOT NULL |

#### 2. movies（电影表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| title | VARCHAR(200) | 电影标题 | NOT NULL |
| original_title | VARCHAR(200) | 原始标题 | NULL |
| aliases | VARCHAR(500) | 别名（JSON数组） | NULL |
| poster_url | VARCHAR(500) | 海报URL | NULL |
| poster_urls | VARCHAR(2000) | 多张海报（JSON数组） | NULL |
| rating | DECIMAL(3,1) | 评分 | NULL |
| rating_source | VARCHAR(50) | 评分来源 | NULL |
| release_date | VARCHAR(100) | 上映日期 | NULL |
| episodes | VARCHAR(50) | 集数 | NULL |
| genres | VARCHAR(200) | 类型（JSON数组） | NULL |
| region | VARCHAR(100) | 地区 | NULL |
| languages | VARCHAR(200) | 语言（JSON数组） | NULL |
| directors | VARCHAR(500) | 导演（JSON数组） | NULL |
| actors | VARCHAR(1000) | 演员（JSON数组） | NULL |
| synopsis | TEXT | 剧情简介 | NULL |
| douban_url | VARCHAR(500) | 豆瓣链接 | NULL |
| zhihu_url | VARCHAR(500) | 知乎链接 | NULL |
| imdb_url | VARCHAR(500) | IMDB链接 | NULL |
| rotten_tomatoes_url | VARCHAR(500) | 烂番茄链接 | NULL |
| tmdb_url | VARCHAR(500) | TMDB链接 | NULL |
| year | VARCHAR(50) | 年份 | NULL |
| genre | VARCHAR(50) | 主要类型 | NULL |
| created_at | DATETIME | 创建时间 | NOT NULL |
| updated_at | DATETIME | 更新时间 | NOT NULL |

#### 3. feeds（动态表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| user_id | BIGINT | 用户ID | FOREIGN KEY, NOT NULL |
| movie_id | BIGINT | 电影ID | FOREIGN KEY, NULL |
| content | TEXT | 动态内容 | NOT NULL |
| rating | DECIMAL(3,1) | 评分 | NULL |
| like_count | INT | 点赞数 | NOT NULL, DEFAULT 0 |
| share_count | INT | 分享数 | NOT NULL, DEFAULT 0 |
| comment_count | INT | 评论数 | NOT NULL, DEFAULT 0 |
| created_at | DATETIME | 创建时间 | NOT NULL |
| updated_at | DATETIME | 更新时间 | NOT NULL |

#### 4. comments（评论表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| feed_id | BIGINT | 动态ID | FOREIGN KEY, NOT NULL |
| user_id | BIGINT | 用户ID | FOREIGN KEY, NOT NULL |
| content | TEXT | 评论内容 | NOT NULL |
| like_count | INT | 点赞数 | NOT NULL, DEFAULT 0 |
| created_at | DATETIME | 创建时间 | NOT NULL |
| updated_at | DATETIME | 更新时间 | NOT NULL |

#### 5. likes（点赞表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| user_id | BIGINT | 用户ID | FOREIGN KEY, NOT NULL |
| feed_id | BIGINT | 动态ID | FOREIGN KEY, NOT NULL |
| created_at | DATETIME | 创建时间 | NOT NULL |

**唯一索引**: (user_id, feed_id)

#### 6. favorites（收藏表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| user_id | BIGINT | 用户ID | FOREIGN KEY, NOT NULL |
| movie_id | BIGINT | 电影ID | FOREIGN KEY, NOT NULL |
| created_at | DATETIME | 创建时间 | NOT NULL |

**唯一索引**: (user_id, movie_id)

#### 7. events（活动表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| title | VARCHAR(200) | 活动标题 | NOT NULL |
| image_url | VARCHAR(500) | 活动图片 | NULL |
| event_date | DATETIME | 活动时间 | NOT NULL |
| location | VARCHAR(200) | 活动地点 | NOT NULL |
| participants | INT | 参与人数 | NOT NULL, DEFAULT 0 |
| max_participants | INT | 最大人数 | NOT NULL |
| type | VARCHAR(50) | 活动类型 | NOT NULL |
| description | TEXT | 活动描述 | NULL |
| movie_id | BIGINT | 关联电影ID | FOREIGN KEY, NULL |
| created_at | DATETIME | 创建时间 | NOT NULL |
| updated_at | DATETIME | 更新时间 | NOT NULL |

#### 8. event_participants（活动参与表）
| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| event_id | BIGINT | 活动ID | FOREIGN KEY, NOT NULL |
| user_id | BIGINT | 用户ID | FOREIGN KEY, NOT NULL |
| joined_at | DATETIME | 参与时间 | NOT NULL |

**唯一索引**: (event_id, user_id)

---

## 核心模块说明

### 1. Controller层（控制器）

#### HelloController
- **功能**: 测试接口
- **路径**: `/hello`
- **说明**: 用于验证项目启动成功

#### TmdbController
- **功能**: TMDB电影数据接口
- **路径**: `/api/tmdb/**`
- **说明**: 提供电影数据查询功能

### 2. Service层（服务）

#### TmdbService
- **功能**: TMDB API集成服务
- **主要方法**:
  - `getPopularMovies()`: 获取热门电影
  - `getMovieDetails()`: 获取电影详情
  - `searchMovies()`: 搜索电影
  - `getMovieCredits()`: 获取演职人员
  - `getNowPlayingMovies()`: 获取正在上映
  - `getTopRatedMovies()`: 获取高分电影

### 3. Repository层（数据访问）

所有Repository接口继承自`JpaRepository`，提供基础CRUD操作。

#### UserRepository
- 用户名查询
- 手机号查询
- 用户名/手机号存在性检查

#### MovieRepository
- 评分排序查询
- 电影搜索（标题、导演、演员）

#### FeedRepository
- 按时间倒序查询
- 按用户查询动态

#### CommentRepository
- 按动态查询评论
- 评论数统计

#### LikeRepository
- 点赞状态检查
- 点赞/取消点赞
- 点赞数统计

#### FavoriteRepository
- 收藏列表查询
- 收藏状态检查
- 收藏/取消收藏

#### EventRepository
- 活动列表查询
- 按电影查询活动

#### EventParticipantRepository
- 参与状态检查
- 参与/取消参与
- 参与人数统计

### 4. Entity层（实体）

所有实体类使用JPA注解，支持自动审计（创建时间、更新时间）。

**关键注解**:
- `@Entity`: 标记为JPA实体
- `@Table`: 指定表名
- `@Data`: Lombok自动生成getter/setter
- `@EntityListeners(AuditingEntityListener.class)`: 启用审计
- `@CreatedDate`: 自动记录创建时间
- `@LastModifiedDate`: 自动记录更新时间

### 5. DTO层（数据传输对象）

#### ApiResponse<T>
统一的API响应格式：
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 6. Security层（安全配置）

#### SecurityConfig
- 禁用CSRF（方便开发测试）
- 放行测试接口和TMDB接口
- 其他接口需要认证（待实现JWT）

### 7. Util层（工具类）

#### TimeUtil
- `getTimeAgo()`: 将时间转换为"X分钟前"格式

---

## API接口列表

### 测试接口

#### GET /hello
- **功能**: 测试接口
- **响应**: "后端项目启动成功！你好，电影社区！"

### TMDB接口

#### GET /api/tmdb/popular
- **功能**: 获取热门电影
- **参数**: `page` (默认1)
- **响应**: TMDB电影列表JSON

#### GET /api/tmdb/movie/{movieId}
- **功能**: 获取电影详情
- **参数**: `movieId` (路径参数)
- **响应**: 电影详情JSON

#### GET /api/tmdb/search
- **功能**: 搜索电影
- **参数**: `keyword`, `page` (默认1)
- **响应**: 搜索结果JSON

#### GET /api/tmdb/movie/{movieId}/credits
- **功能**: 获取演职人员
- **参数**: `movieId` (路径参数)
- **响应**: 演职人员JSON

#### GET /api/tmdb/now-playing
- **功能**: 获取正在上映
- **参数**: `page` (默认1)
- **响应**: 正在上映电影JSON

#### GET /api/tmdb/top-rated
- **功能**: 获取高分电影
- **参数**: `page` (默认1)
- **响应**: 高分电影JSON

---

## 配置说明

### application.properties

#### 服务器配置
```properties
server.port=7070
```

#### 数据库配置
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/final
spring.datasource.username=root
spring.datasource.password=root
```

#### JPA配置
```properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
```

#### JWT配置
```properties
jwt.secret=your-secret-key-change-this-in-production-movie-community-2026
jwt.expiration=604800000
```

#### TMDB配置
```properties
tmdb.api.key=e4cec003c65f908828840d591a4e8311
tmdb.api.read-token=eyJhbGci...
tmdb.api.base-url=https://api.themoviedb.org/3
tmdb.image.base-url=https://image.tmdb.org/t/p
```

#### CORS配置
```properties
spring.web.cors.allowed-origins=http://localhost:*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
```

---

## 开发规范

### 代码规范

1. **文件行数限制**: 单个文件代码不超过1000行
2. **命名规范**:
   - 类名: 大驼峰（PascalCase）
   - 方法名: 小驼峰（camelCase）
   - 常量: 全大写下划线分隔（UPPER_SNAKE_CASE）
3. **注释规范**:
   - 类和方法必须有JavaDoc注释
   - 复杂逻辑必须有行内注释
4. **包结构**: 按功能模块划分，保持清晰的层次结构

### Git提交规范

- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具相关

### 数据库规范

1. 表名使用复数形式（users, movies）
2. 字段名使用下划线分隔（created_at）
3. 必须有主键和时间戳字段
4. 外键关系必须明确定义

---

## 待开发功能

### 高优先级
- [ ] 用户注册/登录接口
- [ ] JWT认证实现
- [ ] 动态发布/查询接口
- [ ] 评论功能接口
- [ ] 点赞功能接口

### 中优先级
- [ ] 收藏功能接口
- [ ] 活动管理接口
- [ ] 用户个人信息接口
- [ ] 文件上传功能

### 低优先级
- [ ] 消息通知功能
- [ ] 搜索优化
- [ ] 数据统计接口
- [ ] 管理后台接口

---

## 前端对接说明

### 前端技术栈
- **Flutter**: 跨平台移动应用框架
- **Dart**: 编程语言

### 前端主要页面
1. **登录/注册页**: `/login`, `/register`
2. **首页**: `/home` (底部导航)
3. **主页**: 动态、榜单、活动、收藏四个Tab
4. **电影详情页**: 显示电影信息、观影团
5. **活动详情页**: 显示活动信息、报名
6. **动态详情页**: 显示动态、评论列表

### 数据模型对应关系

前端Mock数据模型与后端实体的对应关系：

| 前端模型 | 后端实体 | 说明 |
|---------|---------|------|
| FeedItem | Feed | 动态 |
| CommentItem | Comment | 评论 |
| CarouselMovie | Movie | 轮播电影 |
| RankingMovie | Movie | 榜单电影 |
| EventItem | Event | 活动 |
| MovieDetail | Movie | 电影详情 |
| EventDetail | Event | 活动详情 |

---

## 更新日志

### v0.0.1-SNAPSHOT (2026-02-26)
- ✅ 项目初始化
- ✅ 数据库实体设计完成
- ✅ Repository层完成
- ✅ TMDB API集成完成
- ✅ 基础配置完成
- 📝 创建项目架构文档

---

**文档维护**: 本文档将随项目开发自动更新

