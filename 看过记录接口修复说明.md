# 看过记录接口修复说明

## 问题描述

前端在检查电影看过状态时使用的是 TMDB ID，但后端 `/api/watched/check/{movieId}` 接口期望的是本地数据库的电影ID，导致检查结果不准确。

**示例**：
- 前端请求：`GET /api/watched/check/20982`（20982是TMDB ID）
- 后端期望：`GET /api/watched/check/9`（9是本地数据库ID）
- 结果：返回 `isWatched: false`（因为找不到ID为20982的本地电影记录）

## 解决方案

新增一个接口，支持通过 TMDB ID 检查看过状态。

### 新增接口

#### GET /api/watched/check/tmdb/{tmdbId}

检查用户是否看过某部电影（根据TMDB ID）。

**是否需要Token**: ✅ 是

**请求头**:
```
Authorization: Bearer {token}
```

**路径参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| tmdbId | int | 是 | TMDB电影ID |

**请求示例**:
```bash
GET /api/watched/check/tmdb/20982
```

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "isWatched": true
  }
}
```

**使用场景**:
- 前端从TMDB获取电影信息后，使用TMDB ID检查看过状态
- 避免需要先查询本地电影ID的额外步骤

### 原有接口保留

#### GET /api/watched/check/{movieId}

检查用户是否看过某部电影（根据本地ID）。

**路径参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| movieId | long | 是 | 本地数据库电影ID |

**使用场景**:
- 当已知本地电影ID时使用
- 用于收藏夹、活动等场景

## 前端修改建议

将检查看过状态的请求从：
```dart
GET /api/watched/check/20982  // 错误：使用TMDB ID
```

改为：
```dart
GET /api/watched/check/tmdb/20982  // 正确：使用新接口
```

## 代码修改

### 1. WatchedMovieService.java
添加了 `isWatchedByTmdbId` 方法：
```java
public boolean isWatchedByTmdbId(Long userId, Integer tmdbId) {
    return movieRepository.findByTmdbId(tmdbId)
            .map(movie -> watchedMovieRepository.existsByUserIdAndMovieId(userId, movie.getId()))
            .orElse(false);
}
```

### 2. WatchedMovieController.java
添加了新的接口方法：
```java
@GetMapping("/check/tmdb/{tmdbId}")
public ResponseEntity<ApiResponse<Map<String, Boolean>>> checkWatchedByTmdbId(
        @RequestHeader("Authorization") String token,
        @PathVariable Integer tmdbId) {
    Long userId = jwtUtil.extractUserId(token.replace("Bearer ", ""));
    boolean isWatched = watchedMovieService.isWatchedByTmdbId(userId, tmdbId);
    return ResponseEntity.ok(ApiResponse.success(Map.of("isWatched", isWatched)));
}
```

## 测试验证

1. 标记电影为看过：`POST /api/watched` with `{tmdbId: 20982}`
2. 检查看过状态（新接口）：`GET /api/watched/check/tmdb/20982`
3. 预期结果：`{"isWatched": true}`

## 总结

- ✅ 新增 `/api/watched/check/tmdb/{tmdbId}` 接口
- ✅ 保留原有 `/api/watched/check/{movieId}` 接口
- ✅ 前端可以根据场景选择合适的接口
- ✅ 解决了TMDB ID和本地ID混用的问题


