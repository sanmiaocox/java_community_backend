# Spring Boot 电影交流社区后端 - 配置说明

## 已完成的配置

### 1. Maven 镜像配置
已在 `pom.xml` 中配置阿里云 Maven 镜像，加速依赖下载。

### 2. 数据库配置
在 `application.properties` 中配置了 MySQL 数据库连接：
- 数据库名：`final`
- 用户名：`root`
- 密码：`root`
- 端口：`3306`

**请根据你的实际情况修改这些配置！**

### 3. Spring Security 配置
已暂时禁用 Spring Security，方便初期测试。

### 4. 包扫描配置
已修复包名不一致问题，添加了 `@ComponentScan` 注解。

## 运行前准备

### 1. 安装 MySQL 数据库
确保你的电脑上已安装 MySQL 数据库。

### 2. 创建数据库

已经在navicat中的localhost_3306中创建了数据库final
字符集:utf8mb4
排序规则:utf8mb4_0900_ai_ci

### 方式二：使用 IDE
直接运行 `JavaCommunityBackendApplication.java` 的 main 方法。

## 测试项目

项目启动成功后，在浏览器访问：
```
http://localhost:8080/hello
```

如果看到 "后端项目启动成功！你好，电影社区！"，说明配置成功！

## 常见问题

### 1. 依赖下载慢
已配置阿里云镜像，应该会快很多。

### 2. 数据库连接失败
- 检查 MySQL 是否启动
- 检查用户名、密码是否正确
- 检查数据库是否已创建

### 3. 端口被占用
修改 `application.properties` 中的 `server.port` 为其他端口。





