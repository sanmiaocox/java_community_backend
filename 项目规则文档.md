# 电影交流社区项目规则文档

> **最后更新**: 2026-02-26  
> **项目类型**: 毕业设计项目  
> **技术栈**: Flutter + Spring Boot

---

## 📋 目录

1. [项目基本规则](#项目基本规则)
2. [代码开发规范](#代码开发规范)
3. [文档管理规则](#文档管理规则)
4. [前后端协作规则](#前后端协作规则)
5. [数据库规范](#数据库规范)
6. [Git提交规范](#git提交规范)
7. [测试规范](#测试规范)
8. [安全规范](#安全规范)
9. [性能优化规则](#性能优化规则)
10. [用户指令记录](#用户指令记录)

---

## 项目基本规则

### 1. 项目角色定义

- **AI助手角色**: 仅负责后端代码开发和维护
- **前端代码**: 只读参考，不进行修改
- **文档维护**: 自动更新所有项目文档

### 2. 核心原则

#### ✅ 必须遵守
1. **只修改后端代码**，不修改前端Flutter代码
2. **单文件代码不超过1000行**，超过需拆分
3. **保证代码可读性**，添加必要注释
4. **自动更新文档**，代码变更后同步更新相关MD文档
5. **记录用户指令**，当用户说"记住"或"必须"时，记录到本文档

#### ❌ 禁止操作
1. 不修改前端Flutter代码
2. 不删除或重命名已有的数据库表
3. 不随意更改API接口路径（需与前端协商）
4. 不提交包含敏感信息的代码

### 3. 文档自动更新规则

当以下情况发生时，必须更新对应文档：

| 变更类型 | 需更新的文档 |
|---------|-------------|
| 新增/修改实体类 | 项目架构文档.md（数据库设计部分） |
| 新增/修改Controller | 后端API文档.md |
| 新增/修改Service | 项目架构文档.md（核心模块说明） |
| 修改配置文件 | 项目架构文档.md（配置说明） |
| 前端需求变更 | 前端对接建议.md（新建） |
| 用户特殊指令 | 项目规则文档.md（本文档） |

---

## 代码开发规范

### 1. 文件组织规范

#### 包结构
```
com.community.java_community_backend/
├── controller/          # 控制器层（处理HTTP请求）
├── service/            # 服务层（业务逻辑）
├── repository/         # 数据访问层（数据库操作）
├── entity/             # 实体类（数据库表映射）
├── dto/                # 数据传输对象
│   ├── request/        # 请求DTO
│   └── response/       # 响应DTO
├── config/             # 配置类
├── Security/           # 安全配置
├── util/               # 工具类
├── exception/          # 自定义异常
└── constant/           # 常量定义
```

#### 文件命名规范
- Controller: `XxxController.java`
- Service: `XxxService.java` (接口) + `XxxServiceImpl.java` (实现)
- Repository: `XxxRepository.java`
- Entity: `Xxx.java`
- DTO: `XxxRequest.java` / `XxxResponse.java`

### 2. 代码行数限制

**硬性规定**: 单个Java文件不超过1000行

**拆分策略**:
- Controller超过1000行 → 按功能模块拆分多个Controller
- Service超过1000行 → 拆分为多个Service或提取通用方法到Util
- Entity超过1000行 → 检查是否设计不合理，考虑拆分表

### 3. 命名规范

#### 类命名
```java
// ✅ 正确
public class UserController { }
public class MovieService { }
public class FeedRepository { }

// ❌ 错误
public class user_controller { }
public class movieservice { }
```

#### 方法命名
```java
// ✅ 正确
public List<Feed> getFeedList() { }
public void createFeed(FeedRequest request) { }
public boolean isUserExist(String username) { }

// ❌ 错误
public List<Feed> get_feed_list() { }
public void CreateFeed(FeedRequest request) { }
```

#### 变量命名
```java
// ✅ 正确
private String userName;
private int maxParticipants;
private final String API_KEY = "xxx";

// ❌ 错误
private String user_name;
private int MaxParticipants;
private final String apiKey = "xxx";
```

### 4. 注释规范

#### 类注释（必须）
```java
/**
 * 用户服务类
 * 负责用户相关的业务逻辑处理
 * 
 * @author AI Assistant
 * @since 2026-02-26
 */
@Service
public class UserService {
}
```

#### 方法注释（必须）
```java
/**
 * 获取用户动态列表
 * 
 * @param userId 用户ID
 * @param page 页码（从0开始）
 * @param size 每页数量
 * @return 动态列表分页数据
 */
public Page<Feed> getUserFeeds(Long userId, int page, int size) {
}
```

#### 复杂逻辑注释（必须）
```java
// 检查用户是否已点赞，避免重复点赞
if (likeRepository.existsByUserIdAndFeedId(userId, feedId)) {
    throw new BusinessException("已经点赞过了");
}
```

### 5. 代码格式规范

#### 缩进
- 使用4个空格缩进（不使用Tab）

#### 大括号
```java
// ✅ 正确
if (condition) {
    doSomething();
}

// ❌ 错误
if (condition) 
{
    doSomething();
}
```

#### 空行
```java
public class UserService {
    
    private final UserRepository userRepository;
    
    public User getUserById(Long id) {
        return userRepository.findById(id)
            .orElseThrow(() -> new NotFoundException("用户不存在"));
    }
    
    public void createUser(User user) {
        // 验证用户名
        if (userRepository.existsByUsername(user.getUsername())) {
            throw new BusinessException("用户名已存在");
        }
        
        // 保存用户
        userRepository.save(user);
    }
}
```

---

## 文档管理规则

### 1. 必须维护的文档

| 文档名称 | 用途 | 更新时机 |
|---------|------|---------|
| 项目架构文档.md | 项目整体架构说明 | 结构变更时 |
| 后端API文档.md | API接口文档 | 接口变更时 |
| TMDB集成使用指南.md | TMDB API使用说明 | TMDB相关变更时 |
| 项目规则文档.md | 开发规则和约定 | 规则变更时 |
| 前端对接建议.md | 前端修改建议 | 发现前端问题时 |

### 2. 文档更新流程

```
代码变更 → 识别影响范围 → 更新相关文档 → 记录更新日志
```

### 3. 文档格式规范

- 使用Markdown格式
- 使用中文简体
- 代码块必须指定语言
- 表格必须对齐
- 使用emoji增强可读性（适度）

---

## 前后端协作规则

### 1. 接口设计原则

#### 统一响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

#### RESTful规范
- GET: 查询资源
- POST: 创建资源
- PUT: 更新资源（完整更新）
- PATCH: 更新资源（部分更新）
- DELETE: 删除资源

#### URL命名
```
✅ /api/users
✅ /api/feeds/{id}
✅ /api/movies/{id}/comments

❌ /api/getUsers
❌ /api/feed_detail
```

### 2. 前端数据模型对应

后端开发时需参考前端Mock数据结构，确保字段名称和类型一致：

| 前端字段 | 后端字段 | 类型 | 说明 |
|---------|---------|------|------|
| userName | username | String | 用户名 |
| userAvatar | avatar | String | 头像URL |
| timeAgo | createdAt | DateTime | 时间（后端返回时间戳，前端转换） |
| movieTitle | title | String | 电影标题 |
| moviePoster | posterUrl | String | 海报URL |

### 3. 前端问题反馈机制

当发现前端代码问题时：
1. 不直接修改前端代码
2. 在 `前端对接建议.md` 中记录问题和建议
3. 提供详细的修改方案
4. 标注优先级（高/中/低）

---

## 数据库规范

### 1. 表命名规范

- 使用复数形式: `users`, `movies`, `feeds`
- 使用小写字母和下划线: `event_participants`
- 避免使用保留字: 不使用 `order`, `group` 等

### 2. 字段命名规范

- 使用小写字母和下划线: `created_at`, `user_id`
- 主键统一命名为 `id`
- 外键命名为 `关联表_id`: `user_id`, `movie_id`
- 布尔字段使用 `is_` 前缀: `is_active`, `is_deleted`

### 3. 必须字段

每个表必须包含：
- `id`: 主键，BIGINT，自增
- `created_at`: 创建时间，DATETIME
- `updated_at`: 更新时间，DATETIME（如果需要）

### 4. 字段类型选择

| 数据类型 | 数据库类型 | 说明 |
|---------|-----------|------|
| 主键ID | BIGINT | 自增 |
| 短文本 | VARCHAR(n) | n根据实际需求 |
| 长文本 | TEXT | 评论、简介等 |
| 整数 | INT | 计数、状态等 |
| 小数 | DECIMAL(m,n) | 评分、价格等 |
| 日期时间 | DATETIME | 时间戳 |
| 布尔值 | TINYINT(1) | 0/1 |

### 5. 索引规范

- 主键自动创建索引
- 外键字段创建索引
- 频繁查询的字段创建索引
- 唯一约束字段创建唯一索引

```java
// 示例：唯一索引
@Table(name = "users", uniqueConstraints = {
    @UniqueConstraint(columnNames = {"username"}),
    @UniqueConstraint(columnNames = {"phone"})
})
```

---

## Git提交规范

### 1. 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 2. Type类型

| Type | 说明 | 示例 |
|------|------|------|
| feat | 新功能 | feat(user): 添加用户注册接口 |
| fix | 修复bug | fix(feed): 修复动态列表分页错误 |
| docs | 文档更新 | docs: 更新API文档 |
| style | 代码格式 | style: 格式化代码 |
| refactor | 重构 | refactor(service): 重构用户服务 |
| test | 测试 | test(user): 添加用户服务单元测试 |
| chore | 构建/工具 | chore: 更新依赖版本 |

### 3. 提交示例

```bash
# 好的提交
git commit -m "feat(auth): 实现JWT登录认证功能"
git commit -m "fix(feed): 修复点赞数统计错误"
git commit -m "docs: 更新项目架构文档"

# 不好的提交
git commit -m "update"
git commit -m "修改代码"
git commit -m "fix bug"
```

---

## 测试规范

### 1. 单元测试

- 每个Service方法必须有对应的单元测试
- 测试覆盖率目标：80%以上
- 使用JUnit 5 + Mockito

```java
@SpringBootTest
class UserServiceTest {
    
    @Autowired
    private UserService userService;
    
    @MockBean
    private UserRepository userRepository;
    
    @Test
    void testGetUserById() {
        // given
        User user = new User();
        user.setId(1L);
        user.setUsername("test");
        when(userRepository.findById(1L)).thenReturn(Optional.of(user));
        
        // when
        User result = userService.getUserById(1L);
        
        // then
        assertNotNull(result);
        assertEquals("test", result.getUsername());
    }
}
```

### 2. 接口测试

- 使用Postman或curl测试所有API接口
- 测试正常情况和异常情况
- 记录测试用例

### 3. 集成测试

- 测试完整的业务流程
- 使用测试数据库
- 测试后清理数据

---

## 安全规范

### 1. 密码安全

```java
// ✅ 正确：使用BCrypt加密
String encodedPassword = passwordEncoder.encode(rawPassword);

// ❌ 错误：明文存储
user.setPassword(rawPassword);
```

### 2. SQL注入防护

```java
// ✅ 正确：使用参数化查询
@Query("SELECT u FROM User u WHERE u.username = :username")
User findByUsername(@Param("username") String username);

// ❌ 错误：字符串拼接
String sql = "SELECT * FROM users WHERE username = '" + username + "'";
```

### 3. XSS防护

- 对用户输入进行HTML转义
- 使用Spring Security的XSS过滤器

### 4. 敏感信息保护

```properties
# ❌ 错误：硬编码敏感信息
jwt.secret=my-secret-key

# ✅ 正确：使用环境变量
jwt.secret=${JWT_SECRET:default-secret-for-dev}
```

---

## 性能优化规则

### 1. 数据库查询优化

```java
// ✅ 正确：使用分页
Page<Feed> feeds = feedRepository.findAll(PageRequest.of(page, size));

// ❌ 错误：查询所有数据
List<Feed> feeds = feedRepository.findAll();
```

### 2. N+1问题解决

```java
// ✅ 正确：使用JOIN FETCH
@Query("SELECT f FROM Feed f JOIN FETCH f.user WHERE f.id = :id")
Feed findByIdWithUser(@Param("id") Long id);

// ❌ 错误：懒加载导致N+1
Feed feed = feedRepository.findById(id);
String username = feed.getUser().getUsername(); // 触发额外查询
```

### 3. 缓存策略

```java
// 使用Spring Cache
@Cacheable(value = "movies", key = "#id")
public Movie getMovieById(Long id) {
    return movieRepository.findById(id).orElse(null);
}

@CacheEvict(value = "movies", key = "#id")
public void updateMovie(Long id, Movie movie) {
    movieRepository.save(movie);
}
```

---

## 用户指令记录

### 重要指令列表

以下是用户使用"记住"或"必须"等关键词强调的重要指令：

#### 1. 代码修改权限
**指令**: "记住：你不需要修改前端代码，你只能修改后端代码"
- **时间**: 2026-02-26
- **说明**: AI助手只负责后端Java代码，不修改Flutter前端代码
- **状态**: ✅ 已记录

#### 2. 代码行数限制
**指令**: "保证代码的可读性，非必要一个文件中的代码不可超过一千行"
- **时间**: 2026-02-26
- **说明**: 单个Java文件最多1000行，超过需拆分
- **状态**: ✅ 已记录

#### 3. 文档自动更新
**指令**: "当代码改变需要你自行更新md架构文件"
- **时间**: 2026-02-26
- **说明**: 代码变更后自动更新相关文档
- **状态**: ✅ 已记录

#### 4. 前端参考
**指令**: "你也可以查看前端的代码来完善后端也可以对前端代码提出修改建议并且写在md文件中"
- **时间**: 2026-02-26
- **说明**: 可参考前端代码，但只能提建议，不能直接修改
- **状态**: ✅ 已记录

---

### 指令添加模板

当用户使用"记住"或"必须"等关键词时，按以下格式添加：

```markdown
#### N. 指令标题
**指令**: "用户原话"
- **时间**: YYYY-MM-DD
- **说明**: 详细说明
- **状态**: ✅ 已记录 / ⏳ 进行中 / ❌ 已废弃
```

---

## 附录

### 常用命令

#### Maven命令
```bash
# 编译项目
mvn clean compile

# 运行项目
mvn spring-boot:run

# 打包项目
mvn clean package

# 跳过测试打包
mvn clean package -DskipTests
```

#### 数据库命令
```sql
-- 创建数据库
CREATE DATABASE final CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;

-- 查看表结构
DESC users;

-- 查看所有表
SHOW TABLES;
```

---

## 更新日志

### v1.0.0 (2026-02-26)
- ✅ 创建项目规则文档
- ✅ 定义代码开发规范
- ✅ 定义文档管理规则
- ✅ 定义前后端协作规则
- ✅ 记录用户重要指令

---

**文档维护**: 本文档将持续更新，记录所有重要的项目规则和用户指令

